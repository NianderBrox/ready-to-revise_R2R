package com.r2r.readytorevise.data.remote

import com.r2r.readytorevise.BuildConfig

object NetworkConstants {
    // URL is now configured via local.properties (BACKEND_URL)
    const val BASE_URL = BuildConfig.BACKEND_URL
}
